package com.example.studentCourses.Entity;

import java.time.LocalDateTime;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;

@Entity
@Table(name = "StudentCompletedCourses")
public class StudentCompletedCoursesEntity {

	@Id
	@Column(name = "StCoId")
	@GeneratedValue(strategy = GenerationType.IDENTITY)
	private Long sCId;
	
	@Column(name = "Students")
	@ManyToOne
	@JoinColumn(name = "StuId",nullable = false)
	private RegisteredStudentsEntity student;
	
	@ManyToOne
	@JoinColumn(name = "CouId",nullable = false)
	private CoursesEntity course;
	
	@Column(name = "CompltedAt")
	private LocalDateTime completedAt;

	public Long getsCId() {
		return sCId;
	}

	public void setsCId(Long sCId) {
		this.sCId = sCId;
	}

	public RegisteredStudentsEntity getStudent() {
		return student;
	}

	public void setStudent(RegisteredStudentsEntity student) {
		this.student = student;
	}

	public CoursesEntity getCourse() {
		return course;
	}

	public void setCourse(CoursesEntity course) {
		this.course = course;
	}

	public LocalDateTime getCompletedAt() {
		return completedAt;
	}

	public void setCompletedAt(LocalDateTime completedAt) {
		this.completedAt = completedAt;
	}
	
	
	
}
